# Copy to /etc/analytics-assistant/env on the VM (chmod 600).
# Required for LLM queries:
GROQ_API_KEY=gsk_your_key_here

# Optional — only if the UI is on a different origin than the API:
# ALLOWED_ORIGINS=https://your-domain.com
