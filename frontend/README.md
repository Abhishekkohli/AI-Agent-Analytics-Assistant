# Analytics Assistant — Frontend

React (Vite) UI for the AI Agent Analytics Assistant.

## Run

1. Start the API from the project root: `uvicorn api:app --reload --port 8000`
2. In this folder: `npm install && npm run dev`
3. Open http://localhost:5173

`/api` requests are proxied to the FastAPI backend.
